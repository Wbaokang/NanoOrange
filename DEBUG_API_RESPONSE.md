# API 响应调试指南

## 🔍 问题：API响应格式不符合预期

当您看到 "API响应已接收，但响应格式不符合预期" 错误时，这表示 API 返回了响应，但我们无法从中提取图片。

## 📝 调试步骤

### 1. 查看服务器日志

在运行 `npm run dev` 的终端中，查找以下日志：

```
开始解析API响应...
响应消息: {...}
内容类型: ...
是否为数组: ...
```

这些日志会显示 API 实际返回的数据结构。

### 2. 查看浏览器控制台

在浏览器中打开开发者工具（F12），查看 Console 标签页：

- 查找 "API响应格式问题" 错误
- 查看 "原始响应" 和 "响应内容" 的详细信息

### 3. 检查 API 响应格式

根据日志信息，API 可能返回以下格式之一：

#### 格式 A：数组格式
```json
{
  "content": [
    {
      "type": "image_url",
      "image_url": {
        "url": "data:image/png;base64,..."
      }
    }
  ]
}
```

#### 格式 B：字符串格式（Base64）
```json
{
  "content": "data:image/png;base64,iVBORw0KGgo..."
}
```

#### 格式 C：文本描述
```json
{
  "content": "这是一段文本描述..."
}
```

#### 格式 D：图片URL
```json
{
  "content": "https://example.com/image.png"
}
```

## 🔧 解决方案

### 方案 1：检查 API 密钥和配额

1. 确认 API 密钥有效且有足够配额
2. 访问 https://openrouter.ai/settings/keys 检查状态

### 方案 2：查看实际响应

1. 在服务器终端中查找 "API响应:" 日志
2. 复制完整的响应 JSON
3. 根据实际格式调整解析逻辑

### 方案 3：测试不同的提示词

某些提示词可能导致 API 返回文本而不是图片。尝试：

- 更具体的图像编辑指令
- 明确的图像生成请求
- 参考 OpenRouter 文档中的示例

### 方案 4：检查模型能力

Gemini 2.5 Flash Image 是一个图像生成模型，但：
- 它可能在某些情况下返回文本描述
- 它可能需要特定的提示词格式
- 它可能对输入图像有特定要求

## 📚 参考文档

- [OpenRouter Gemini 2.5 Flash Image 文档](https://openrouter.ai/google/gemini-2.5-flash-image/api)
- [OpenRouter API 文档](https://openrouter.ai/docs)

## 💡 常见问题

**Q: 为什么 API 返回文本而不是图片？**
A: 可能是提示词不够明确，或者模型理解为您想要文本描述。尝试使用更明确的图像生成指令。

**Q: 如何查看完整的 API 响应？**
A: 查看服务器终端日志，查找 "API响应:" 开头的日志行。

**Q: API 响应格式会变化吗？**
A: 是的，不同情况下 API 可能返回不同格式。我们的代码已经尝试处理多种格式。

## 🐛 报告问题

如果问题持续存在，请提供以下信息：

1. 服务器终端中的完整日志（特别是 "API响应:" 部分）
2. 浏览器控制台中的错误信息
3. 使用的提示词
4. 上传的图片类型和大小

